import { http } from '@/shared/lib/http'; import type { PageResponse } from '@/shared/types'; import { AI_COACHING_ROUTES } from './routes'; import type { GenerateWeeklyCoachReportRequest, WeeklyCoachHistoryFilter, WeeklyCoachReport, WeeklyCoachReportListItem } from '../types';
export const generateWeeklyCoachReport = (token: string, data: GenerateWeeklyCoachReportRequest) => http<WeeklyCoachReport>(AI_COACHING_ROUTES.reports, { method: 'POST', accessToken: token, json: data }); export const getWeeklyCoachReport = (token: string, id: string) => http<WeeklyCoachReport>(AI_COACHING_ROUTES.report(id), { accessToken: token }); export const getWeeklyCoachReports = (token: string, filter?: WeeklyCoachHistoryFilter) => http<PageResponse<WeeklyCoachReportListItem>>(AI_COACHING_ROUTES.reports, { accessToken: token, query: filter ? { page: filter.page, pageSize: filter.pageSize } : undefined }); export const deleteWeeklyCoachReport = (token: string, id: string) => http<void>(AI_COACHING_ROUTES.report(id), { method: 'DELETE', accessToken: token });
